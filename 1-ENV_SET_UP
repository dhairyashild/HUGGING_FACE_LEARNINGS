# 1. Create venv (skip pip to avoid error)
python -m venv venv --without-pip

# 2. Activate venv (use isolated environment)
source venv/Scripts/activate

# 3. Install pip (needed for package management)
python -m ensurepip

# 4. Upgrade pip (get latest version)
python -m pip install --upgrade pip

# 5. Install all dependencies (LangChain, HuggingFace, etc.)              ///// ADD ALL DEPENDENCIES IN requirement.txt in 1 line each word
pip install langchain langchain-core langchain-openai openai langchain-community transformers huggingface-hub sentence-transformers torch accelerate python-dotenv numpy scikit-learn

# 6. Verify (check installed packages)
pip list | grep -E "langchain|transformers|torch"


from langchain_huggingface                import ChatHuggingFace,           HuggingFaceEndpoint
#      ^^^^^^^^^^^^^^^^^^^                     ^^^^^^^^^^^^^^^              ^^^^^^^^^^^^^^^^^^^^
#     snake_case(all lowercase)                 PascalCase                     PascalCase class
#         module                                   class                            class                 

1. snake_case
user_input_data = lowercase with underscores.
2. PascalCase
UserInputData = first letter of each word capital.
3. camelCase
userInputData = first word lowercase, rest capital.
4. SCREAMING_SNAKE_CASE
USER_INPUT_DATA = uppercase with underscores.




For next time (daily work):
# Activate existing venv
source venv/Scripts/activate

# Deactivate when done
deactivate
